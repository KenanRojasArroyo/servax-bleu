# Build outputs
[Bb]in/
[Oo]bj/
*.pdb
*.dll

# Visual Studio
.vs/
*.user
*.suo

# NuGet
packages/
*.nupkg

# Web.config contiene credenciales reales (SMTP, cadena de conexión).
# Se sube Web.config.example como plantilla; Web.config real NUNCA se commitea.
**/Web.config
!**/Web.config.example

# Web.Debug.config / Web.Release.config pueden quedarse si no traen secretos,
# pero revísalos antes de confirmar.